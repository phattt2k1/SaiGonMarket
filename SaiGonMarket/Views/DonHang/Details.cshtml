@model SaiGonMarket.ModelViews.XemDonHang
@{
    List<SaiGonMarket.ModelViews.CartItem> Carts = ViewBag.GioHang;
}
<h3>Thông tin đơn hàng: #@Model.DonHang.OrderId</h3>
<br />
<p>Ngày đặt hàng: @Model.DonHang.OrderDate</p>
<p>Trạng thái đơn hàng: @Model.DonHang.TransactStatus.Status</p>
<p>Tổng giá trị đơn hàng: <strong><span class="amount">@Model.ChiTietDonHang.Sum(x => x.TotalMoney).Value.ToString("#,##0") VNĐ</span></strong></p>
<p>Ngày ship hàng: @Model.DonHang.ShipDate</p>

<div class="table-responsive">
    <table class="table table-bordered table-hover">
        <tbody>
            <tr>
                <th>#</th>
                <th>Sản phẩm</th>
                <th>Số lượng</th>
                <th>Đơn giá</th>
            </tr>

            @foreach (var item in Model.ChiTietDonHang)
            {
                
                <tr>
                    <td>@item.ProductId</td>
                    <td>@item.Product.ProductName</td>
                    <td>@item.Amount</td>
                    <td>@item.Price.Value.ToString("#,##0") VNĐ</td>
                </tr>
            }

        </tbody>
    </table>
</div>
